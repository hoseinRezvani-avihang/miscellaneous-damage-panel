import { ThousandSeperatorDirective } from './thousand-seperator.directive';

describe('ThousandSeperatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThousandSeperatorDirective();
    expect(directive).toBeTruthy();
  });
});
