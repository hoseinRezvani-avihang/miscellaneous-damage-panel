import { IranYekanDirective } from './iran-yekan.directive';

describe('IranYekanDirective', () => {
  it('should create an instance', () => {
    const directive = new IranYekanDirective();
    expect(directive).toBeTruthy();
  });
});
